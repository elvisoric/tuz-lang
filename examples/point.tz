extern fn printf(s: *u8) -> i32;

struct Point {
    x: i32,
    y: i32
}

fn main() -> i32 {

    let mut p: Point;
    p.x = 100;
    p.y = 200;

    for i = 1, 100 {
        p.x = i;
        p.y = 2 * i;
        printf("Point %d: = (%d, %d)\n", i,  p.x, p.y);
    }
   
}
